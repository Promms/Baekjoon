#include <iostream>
#include <string>
#include <unordered_set>
using namespace std;

int main(){
    ios_base::sync_with_stdio(NULL);
    cin.tie(NULL);
    cout.tie(NULL);

    string input;
    cin >> input;
    int length = static_cast<int>(input.size());

    for(int i = 0; i < length; i++){
        for(int j = 0; j + i < length; j++){

        }
    }
    return 0;
}

// 벡터에 하나씩 요소들을 모두 입력한 후 